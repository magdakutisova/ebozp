<?php echo $this->filters()?>

<div class="box" id="filtered">
	
	<?php $subDb = new Application_Model_DbTable_Subsidiary(); 
	//Zend_Debug::dump($this->clients);
	//die();
	foreach($this->clients as $client) : ?>
		<p class="bold">
			<a href="<?php echo $this->url(array('clientId' => $client->getIdClient()), 'clientIndex')?>">
				<?php echo $client->getCompanyName();?>
			</a>
		</p>
		<?php $subsidiaries = $client->getSubsidiaries();
		if ($subsidiaries != 0) {?>
		<ul>
			<?php foreach($subsidiaries as $subsidiaryId) :
				$subsidiary = $subDb->getSubsidiary($subsidiaryId);?>
				<li><?php echo '<a href="' . $this->url(array('clientId' => $client->getIdClient(), 'subsidiary' => $subsidiary->getIdSubsidiary()), 'subsidiaryIndex') . '">' . $subsidiary->getSubsidiaryName() . '</a>, ' . $subsidiary->getSubsidiaryTown(); ?></li>
				<?php endforeach; } ?>
				</ul>
	<?php endforeach; ?>
		
	
</div>