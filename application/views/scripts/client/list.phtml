<?php echo $this->filters()?>

<div class="box" id="filtered">
	
	<?php foreach($this->clients as $client) : ?>
		<p class="bold">
			<a href="<?php echo $this->url(array('clientId' => $client->id_client), 'clientIndex')?>">
				<?php echo $client->company_name;?>
			</a>
		</p>
		<?php $subsidiaries = $client->getSubsidiaries();
		$rowCount = count($subsidiaries);
		if ($rowCount > 0) {?>
		<ul>
			<?php foreach($subsidiaries as $subsidiary) :?>
				<li><?php echo '<a href="' . $this->url(array('clientId' => $client->id_client, 'subsidiary' => $subsidiary->id_subsidiary), 'subsidiaryIndex') . '">' . $subsidiary->subsidiary_name . '</a>, ' . $subsidiary->subsidiary_town; ?></li>
				<?php endforeach; } ?>
				</ul>
	<?php endforeach; ?>
		
	
</div>