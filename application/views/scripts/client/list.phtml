<h2>Výběr klienta</h2>
<div class="box">Různé filtry - název, BT, KOO, město/obec, naposledy otevřené</div>
<div class="box">
	
	<?php foreach($this->clients as $client) : ?>
		<p class="bold"><?php echo $client->company_name;?></p>
		<?php $subsidiaries = $client->getSubsidiaries();
		$rowCount = count($subsidiaries);
		if ($rowCount > 0) {?>
		<ul>
			<?php foreach($subsidiaries as $subsidiary) :
				if(!$subsidiary->hq) {?>
				<li><?php echo $subsidiary->subsidiary_name; ?></li>
				<?php }?>	
				<?php endforeach; } ?>
				</ul>
	<?php endforeach; ?>
		
	
</div>