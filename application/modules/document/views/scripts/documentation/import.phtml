<?php
if ($this->dotNet) {
	echo "1";
} else {
	$redirector = new Zend_Controller_Action_Helper_Redirector();
	$redirector->gotoRouteAndExit(array("clientId" => $this->client->id_client, "TYPE" => $this->REQ_TYPE), "document-documentation-index");
}